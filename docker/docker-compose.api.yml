version: "3.9"
services:
  api:
    build:
      context: ..
      dockerfile: docker/Dockerfile.api
    env_file: ../.env
    volumes:
      - ../runtime:/app/runtime
      - ../data:/app/data
    ports:
      - "8000:8000"
